services:
  - type: web
    name: p6-schedule-compare
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app.main:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: OPENROUTER_API_KEY
        sync: false
